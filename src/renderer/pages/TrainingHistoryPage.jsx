import React from 'react';
import TrainingHistory from '../features/training/TrainingHistory';

export default function TrainingHistoryPage({ initialCustomer }) {
    return <TrainingHistory initialCustomer={initialCustomer} />;
}
